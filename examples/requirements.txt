# LetsCloud API Client Requirements
# Python dependencies for the LetsCloud API client examples

# Core HTTP client library
requests>=2.31.0

# JSON handling (built-in with Python 3.7+)
# json - built-in module

# UUID generation (built-in with Python 3.7+)
# uuid - built-in module

# Type hints (built-in with Python 3.5+)
# typing - built-in module

# Optional: For better JSON formatting
# json5>=0.9.0

# Optional: For environment variable management
python-dotenv>=1.0.0

# Optional: For configuration management
pyyaml>=6.0

# Optional: For CLI argument parsing
click>=8.0.0

# Optional: For colored terminal output
colorama>=0.4.6

# Optional: For progress bars
tqdm>=4.65.0
